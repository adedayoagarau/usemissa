{
  "name": "@missa/radar-engine",
  "version": "0.1.0",
  "description": "Missa Radar — opportunity intelligence engine: discovery, monitoring, change detection, scoring, prediction, matching, and alerts for users and organizations.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "bin": {
    "missa-radar": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test \"dist/test/*.test.js\"",
    "demo": "npm run build && node dist/src/cli.js demo",
    "registry:generate": "node scripts/generate-registry.mjs && npm run build && node dist/src/registry/export-json.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
