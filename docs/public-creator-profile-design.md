# Public creator profile

Implementation: public-creator-profile.tsx and its CSS module. Routes /profile/[userId] and /@handle retain their existing resolution and privacy guards. Only the existing public name/bio projection reaches live pages. Private profiles render no identity or work. Selected work and credits are optional public presentation fields, demonstrated only by explicitly fictional fixtures at /design-system/creator-profile-v2. No portfolio publishing/storage is added.

Composition: identity, optional section links, selected work, credits, quiet discovery footer. Installed Card and native links; semantic colors and responsive two-column work cards. Empty profiles show an honest empty state. Text stays left-aligned. Current preview demonstrates text excerpts; image/audio/video work and backend publishing remain subsequent work. Browser navigation timed out during review; visual QA is outstanding.
