{
  "name": "usemissa",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@missa/radar-engine && npm run build --workspace=@missa/radar-adapters",
    "test": "npm test --workspace=@missa/radar-engine && npm test --workspace=@missa/radar-adapters"
  }
}
