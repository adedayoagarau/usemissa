import previewStyles from "@/components/design-system/opportunities-browse-v2-preview.module.css";

/** Shipped public-index layout contract while the legacy stylesheet is renamed. */
export const publicIndexLayoutStyles = previewStyles;
