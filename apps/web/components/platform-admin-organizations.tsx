'use client';

import { useMemo, useState } from 'react';
import Link from 'next/link';
import { DataAreaHeader, MaturityBadge, MetricCard, WarningList } from '@/components/platform-admin';
import type { AdminArea } from '@/lib/platformAdmin';
import type { PlatformAdminOrganizationsData } from '@/lib/platformAdminContinuation';

function value(value: number | null): string {
  return value === null ? '—' : String(value);
}

export default function PlatformAdminOrganizations({ area }: { area: AdminArea<PlatformAdminOrganizationsData> }) {
  const [search, setSearch] = useState('');
  const [billingStatus, setBillingStatus] = useState('all');
  const rows = useMemo(() => area.data.rows.filter((row) => {
    const haystack = `${row.name} ${row.id} ${row.domains.join(' ')}`.toLowerCase();
    return (!search.trim() || haystack.includes(search.trim().toLowerCase())) && (billingStatus === 'all' || row.billingStatus === billingStatus);
  }), [area.data.rows, billingStatus, search]);

  return <div className="space-y-8">
    <DataAreaHeader area={area} title="Organizations" description="Cross-tenant workflow snapshots for platform operators. This surface observes organization configuration and product activity without impersonating members or exposing private submission content." />
    <WarningList warnings={area.warnings} />
    <section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5" aria-label="Organization summary">
      <MetricCard label="Organizations" value={value(area.data.summary.organizationCount)} detail="Observed organization records" />
      <MetricCard label="Active" value={value(area.data.summary.activeOrganizationCount)} detail="At least one active membership" />
      <MetricCard label="Published calls" value={value(area.data.summary.publishedOpenCallCount)} detail="Published organization open calls" />
      <MetricCard label="Pending delivery" value={value(area.data.summary.pendingDeliveryCount)} detail="Accepted Work tasks still pending" href="/admin/operations?queue=workspace" />
      <MetricCard label="Past due" value={value(area.data.summary.pastDueCount)} detail="Observed billing status only" />
    </section>
    <section className="border border-border bg-card" aria-labelledby="organizations-list-title">
      <div className="border-b border-border px-4 py-4 sm:px-5">
        <div className="flex flex-wrap items-end justify-between gap-3"><div><h2 id="organizations-list-title" className="text-lg font-semibold tracking-tight text-foreground">Organization workflow snapshot</h2><p className="mt-1 text-sm text-muted-foreground">{rows.length} of {area.data.rows.length} organizations shown · no member emails, answers, files, or provider IDs.</p></div><MaturityBadge maturity={area.provenance.maturity} /></div>
        <div className="mt-4 grid gap-2 sm:grid-cols-[minmax(0,1fr)_180px]"><label className="sr-only" htmlFor="organization-search">Search organizations</label><input id="organization-search" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search name, ID, or domain…" className="min-h-11 border border-border bg-card px-3 text-sm text-foreground outline-none placeholder:text-muted-foreground focus:border-primary focus:ring-2 focus:ring-primary/20" /><label className="sr-only" htmlFor="organization-billing-status">Billing status</label><select id="organization-billing-status" value={billingStatus} onChange={(event) => setBillingStatus(event.target.value)} className="min-h-11 border border-border bg-card px-3 text-sm text-foreground outline-none focus:border-primary focus:ring-2 focus:ring-primary/20"><option value="all">All billing states</option><option value="unknown">Unknown</option><option value="inactive">Inactive</option><option value="trialing">Trialing</option><option value="active">Active</option><option value="past_due">Past due</option><option value="canceled">Canceled</option></select></div>
      </div>
      {rows.length === 0 ? <p className="px-5 py-12 text-center text-sm text-muted-foreground">No organizations match the current filters.</p> : <><ul className="divide-y divide-border md:hidden" aria-label="Platform organization workflow snapshot">{rows.map((row) => <li key={row.id} className="space-y-4 px-4 py-5"><div><div className="flex flex-wrap items-center gap-2"><p className="font-semibold text-foreground">{row.name}</p>{row.verified && <span className="text-xs font-medium text-success">Verified</span>}</div><p className="mt-1 break-all font-mono text-xs text-muted-foreground">{row.id}</p>{row.domains.length > 0 && <p className="mt-1 break-words text-sm text-muted-foreground">{row.domains.join(', ')}</p>}</div><dl className="grid gap-x-4 gap-y-3 sm:grid-cols-2"><div><dt className="text-xs font-medium text-muted-foreground">People / seats</dt><dd className="mt-1 text-sm text-foreground"><span className="font-mono">{value(row.memberCount)} / {value(row.seatLimit)}</span> · {value(row.activeMemberCount)} active</dd></div><div><dt className="text-xs font-medium text-muted-foreground">Open calls</dt><dd className="mt-1 text-sm text-foreground"><span className="font-mono">{value(row.openCallCount)}</span> · {value(row.publishedOpenCallCount)} published</dd></div><div><dt className="text-xs font-medium text-muted-foreground">Submissions</dt><dd className="mt-1 text-sm text-foreground"><span className="font-mono">{value(row.submissionCount)}</span> · {value(row.inReviewCount)} in review</dd></div><div><dt className="text-xs font-medium text-muted-foreground">Decisions</dt><dd className="mt-1 text-sm text-foreground"><span className="font-mono">{value(row.decisionCount)}</span> · {value(row.acceptedWorkCount)} accepted</dd></div><div><dt className="text-xs font-medium text-muted-foreground">Delivery</dt><dd className={`mt-1 text-sm ${row.pendingDeliveryCount ? 'text-warning' : 'text-foreground'}`}><span className="font-mono">{value(row.pendingDeliveryCount)}</span> pending · {row.pendingDeliveryCount ? 'needs attention' : 'no pending tasks'}</dd></div><div><dt className="text-xs font-medium text-muted-foreground">Billing</dt><dd className="mt-1 text-sm capitalize text-foreground">{row.billingTier} · {row.billingStatus.replaceAll('_', ' ')} · Connect {row.stripeConnectStatus.replaceAll('-', ' ')}</dd></div><div className="sm:col-span-2"><dt className="text-xs font-medium text-muted-foreground">Latest observed</dt><dd className="mt-1 break-words font-mono text-xs text-foreground">{row.latestObservedAt ?? 'Not observed'}</dd></div></dl><Link href={`/admin/content?organizationId=${encodeURIComponent(row.id)}`} className="inline-flex min-h-11 items-center text-sm font-medium text-accent-deep underline decoration-accent-tint underline-offset-4 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary">Inspect content</Link></li>)}</ul><div className="hidden overflow-x-auto md:block"><table className="w-full min-w-[1180px] border-collapse text-left text-sm"><caption className="sr-only">Platform organization workflow snapshot</caption><thead className="border-b border-border bg-muted/40 text-xs text-muted-foreground"><tr><th scope="col" className="px-4 py-3 font-medium">Organization</th><th scope="col" className="px-4 py-3 font-medium">People / seats</th><th scope="col" className="px-4 py-3 font-medium">Organization</th><th scope="col" className="px-4 py-3 font-medium">Submissions</th><th scope="col" className="px-4 py-3 font-medium">Decisions</th><th scope="col" className="px-4 py-3 font-medium">Delivery</th><th scope="col" className="px-4 py-3 font-medium">Billing</th><th scope="col" className="px-4 py-3 font-medium">Observed</th></tr></thead><tbody>{rows.map((row) => <tr key={row.id} className="border-b border-border align-top last:border-0 hover:bg-muted/20"><th scope="row" className="max-w-[250px] px-4 py-3 font-medium text-foreground"><span className="block truncate">{row.name}</span><span className="mt-1 block truncate font-mono text-[11px] font-normal text-muted-foreground">{row.id}</span>{row.verified && <span className="mt-1 inline-block text-[11px] font-normal text-success">Verified</span>}{row.domains.length > 0 && <span className="mt-1 block truncate text-[11px] font-normal text-muted-foreground">{row.domains.join(', ')}</span>}<Link href={`/admin/content?organizationId=${encodeURIComponent(row.id)}`} className="mt-2 inline-block text-xs font-medium text-accent-deep underline decoration-accent-tint underline-offset-4">Inspect content</Link></th><td className="px-4 py-3 text-xs text-muted-foreground"><span className="font-mono text-foreground">{value(row.memberCount)} / {value(row.seatLimit)}</span><span className="mt-1 block">{value(row.activeMemberCount)} active</span></td><td className="px-4 py-3 text-xs text-muted-foreground"><span className="font-mono text-foreground">{value(row.openCallCount)} calls</span><span className="mt-1 block">{value(row.publishedOpenCallCount)} published</span></td><td className="px-4 py-3 text-xs text-muted-foreground"><span className="font-mono text-foreground">{value(row.submissionCount)}</span><span className="mt-1 block">{value(row.inReviewCount)} in review</span></td><td className="px-4 py-3 text-xs text-muted-foreground"><span className="font-mono text-foreground">{value(row.decisionCount)}</span><span className="mt-1 block">{value(row.acceptedWorkCount)} accepted</span></td><td className="px-4 py-3 text-xs text-muted-foreground"><span className={`font-mono ${row.pendingDeliveryCount ? 'text-warning' : 'text-foreground'}`}>{value(row.pendingDeliveryCount)} pending</span><span className="mt-1 block">{row.pendingDeliveryCount ? 'Needs attention' : 'No pending tasks'}</span></td><td className="px-4 py-3 text-xs text-muted-foreground"><span className="block capitalize text-foreground">{row.billingTier}</span><span className="mt-1 block">{row.billingStatus.replaceAll('_', ' ')}</span><span className="mt-1 block">Connect: {row.stripeConnectStatus.replaceAll('-', ' ')}</span></td><td className="whitespace-nowrap px-4 py-3 font-mono text-[11px] text-muted-foreground">{row.latestObservedAt ?? 'Not observed'}</td></tr>)}</tbody></table></div></>}
    </section>
    <section className="border-t border-border pt-6"><h2 className="text-lg font-semibold tracking-tight text-foreground">Planned CRM boundary</h2><p className="mt-1 text-sm text-muted-foreground">The current page is an observed read model. These capabilities need durable models, action-level authorization, idempotency, and audit contracts before becoming controls.</p><ul className="mt-3 grid gap-2 text-sm text-muted-foreground sm:grid-cols-2">{area.data.planned.map((item) => <li key={item} className="border border-border bg-card px-3 py-2">{item}</li>)}</ul></section>
  </div>;
}
