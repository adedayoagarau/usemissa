import { ApplicationPreparationPreview } from "@/components/application-preparation-preview";

export const metadata = { title: "Prepare a submission · Missa", robots: { index: false, follow: false } };

export default function Page() { return <ApplicationPreparationPreview />; }
